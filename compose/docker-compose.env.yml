services:
  ingestion-service:
    environment:
      SERVICE_NAME: "ingestion-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
      INGEST_API_KEY: "${INGEST_API_KEY:-changeme}"
      SEARCH_INDEX_URL: "${SEARCH_INDEX_URL:-http://search-index-service:8000}"
  evidence-store:
    environment:
      SERVICE_NAME: "evidence-store"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
      EVIDENCE_ROOT: "${EVIDENCE_ROOT:-/data}"
  esg-service:
    environment:
      SERVICE_NAME: "esg-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  time-series-service:
    environment:
      SERVICE_NAME: "time-series-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  emission-factors-service:
    environment:
      SERVICE_NAME: "emission-factors-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  data-quality-service:
    environment:
      SERVICE_NAME: "data-quality-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  kpi-calculation-service:
    environment:
      SERVICE_NAME: "kpi-calculation-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  report-compiler-service:
    environment:
      SERVICE_NAME: "report-compiler-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
      JOBS_SERVICE_URL: "${JOBS_SERVICE_URL:-http://jobs-service:8000}"
      REPORTS_COMPAT_LOG: "${REPORTS_COMPAT_LOG:-1}"
      REPORTS_COMPAT_SUNSET: "${REPORTS_COMPAT_SUNSET:-}"
  xbrl-mapping-service:
    environment:
      SERVICE_NAME: "xbrl-mapping-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  dashboards-service:
    environment:
      SERVICE_NAME: "dashboards-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  search-index-service:
    environment:
      SERVICE_NAME: "search-index-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"
  jobs-service:
    environment:
      SERVICE_NAME: "jobs-service"
      PORT: "${PORT:-8000}"
      LOG_LEVEL: "${LOG_LEVEL:-info}"