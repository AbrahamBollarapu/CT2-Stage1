services:
  emission-factors-service:
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8000/health | grep -q '\"ok\":true' || exit 1"]
      interval: 10s
      timeout: 3s
      start_period: 10s
      retries: 5
